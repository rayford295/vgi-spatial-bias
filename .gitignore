# Large / regenerable data — the notebook downloads the .laz from I-GUIDE and
# recreates these rasters/caches on run, so they don't belong in git.
*.laz
*.las
*.zip
results/**/*.tif
results/**/*.npz
data/NAIP_image.tif
data/lidar_tiles/
data/statewide/

# macOS / editor junk
.DS_Store
__MACOSX/
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
