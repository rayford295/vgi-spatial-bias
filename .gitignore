# Large / regenerable data — the notebook downloads the .laz from I-GUIDE and
# recreates these rasters/caches on run, so they don't belong in git.
*.laz
*.las
*.zip
results/**/*.tif
results/**/*.npz
data/uiuc_campus/NAIP_image.tif
data/uiuc_campus/lidar_tiles/
data/statewide_il/
data/**/*.tif

# macOS / editor junk
.DS_Store
__MACOSX/
.ipynb_checkpoints/

# Python
__pycache__/
*.pyc
